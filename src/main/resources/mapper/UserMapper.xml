<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.Gruuy.dao.UserDao" >
  <select id="getAll" resultType="com.Gruuy.entity.User">
    select * from user
  </select>
  <select id="chechLogin" parameterType="User" resultType="integer">
    select count(*) from user where
  </select>
</mapper>